<h1>List of Airports</h1>

<h2><%= pluralize(@airports.count, "Airport") + "found" %></h2>
<ul>
<% @airports.each do |airport| %>
    <li><%= link_to airport.name, airport_path(airport) %></li>
    <% end %>
</ul>
